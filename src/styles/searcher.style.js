import {StyleSheet} from 'react-native';

export const searcher = StyleSheet.create({
  card: {
    margin: 6,
    padding: 12,
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },
});
